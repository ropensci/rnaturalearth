# Check whether to install rnaturalearthdata and install if necessary

If the rnaturalearthdata package is not installed, install it from
GitHub using pak. If it is not up to date, reinstall it.

## Usage

``` r
check_rnaturalearthdata()
```
