# check that this scale is present in Natural Earth

check name or numeric scale representations, return numeric one

## Usage

``` r
check_scale(x)
```

## Arguments

- x:

  scale of map to return, one of `110`, `50`, `10` or `'small'`,
  `'medium'`, `'large'`

## Value

integer scale of map
